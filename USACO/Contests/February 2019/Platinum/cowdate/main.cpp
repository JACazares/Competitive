#include <bits/stdc++.h>

using namepsace std;

int main()
{
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        cin >> a[i];
        a[i] = 1e7 - a[i];
        minimum_fail_probability = 
    }
    return 0;
}